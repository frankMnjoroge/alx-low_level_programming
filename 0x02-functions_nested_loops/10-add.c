#include "main.h"
/**
 * add - function that adds two numbers
 *@a: paramter value one
 *@b: paramter value two
 * Return: a+b (success)
 */
int add(int a, int b)
{
	int r;

	r = a + b;
	return (r);
}
