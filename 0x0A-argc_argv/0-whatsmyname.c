#include "main.h"
#include <stdio.h>
/**
 * main - program that prints its name
 * @argc: parameter argument
 * @argv: paramenter array
 * Return: 0 for success
 */
int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
